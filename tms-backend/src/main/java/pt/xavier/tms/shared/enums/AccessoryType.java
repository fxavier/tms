package pt.xavier.tms.shared.enums;

/**
 * Standard accessory types for a vehicle (Requisito 2.1).
 */
public enum AccessoryType {

    /** Car jack. */
    MACACO,

    /** Spare wheel. */
    RODA_SOBRESSALENTE,

    /** Warning triangle. */
    TRIANGULO,

    /** Fire extinguisher. */
    EXTINTOR,

    /** First-aid kit. */
    KIT_PRIMEIROS_SOCORROS,

    /** Reflective vest. */
    COLETE_REFLETOR,

    /** Any additional accessory type configured per vehicle type (Requisito 2.2). */
    OUTRO
}
